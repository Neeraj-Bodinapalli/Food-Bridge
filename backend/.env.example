PORT=4000
CORS_ORIGIN=http://localhost:5173
# No password in URL while POSTGRES_HOST_AUTH_METHOD=trust (local Docker only).
DATABASE_URL=postgresql://foodbridge@127.0.0.1:5433/foodbridge
JWT_SECRET=change-me-in-production
